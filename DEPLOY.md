# 🚀 Деплой на Coolify

Инструкция по развертыванию магазина товаров для попугаев на Coolify.

## Подготовка

### 1. Репозиторий
Убедитесь, что проект находится в Git-репозитории (GitHub, GitLab, Gitea и т.д.)

```bash
git add .
git commit -m "Prepare for Coolify deployment"
git push
```

### 2. Переменные окружения
В Coolify настройте следующие переменные:

**Обязательные:**
- `NODE_ENV=production`
- `PORT=5000`
- `DATABASE_PATH=/app/data/parrot_shop.db`

**Опциональные (для Telegram уведомлений):**
- `TELEGRAM_BOT_TOKEN=ваш_токен_бота`
- `TELEGRAM_CHAT_ID=ваш_chat_id`

## Настройка в Coolify

### Вариант 1: Docker Compose (рекомендуется)

1. В Coolify создайте новый проект
2. Выберите "Docker Compose"
3. Подключите ваш Git-репозиторий
4. Coolify автоматически обнаружит `docker-compose.yml`
5. Добавьте переменные окружения
6. Настройте volume для базы данных: `./data:/app/data`
7. Нажмите "Deploy"

### Вариант 2: Dockerfile

1. В Coolify создайте новый проект
2. Выберите "Dockerfile"
3. Подключите ваш Git-репозиторий
4. Coolify автоматически обнаружит `Dockerfile`
5. Добавьте переменные окружения
6. Настройте persistent volume:
   - Source: `/app/data`
   - Destination: volume для SQLite базы
7. Нажмите "Deploy"

### Вариант 3: Nixpacks (без Docker)

1. В Coolify создайте новый проект
2. Выберите "Nixpacks"
3. Подключите ваш Git-репозиторий
4. Добавьте переменные окружения
5. Build Command: `npm run build`
6. Start Command: `npm start`
7. Настройте persistent volume для `/app/data`
8. Нажмите "Deploy"

## Важные настройки

### Persistent Storage
База данных SQLite должна храниться в persistent volume:
- Путь в контейнере: `/app/data`
- Файл базы: `parrot_shop.db`

### Порты
- Приложение слушает порт `5000`
- Coolify автоматически настроит reverse proxy

### Health Check
Coolify может проверять доступность по:
- URL: `http://localhost:5000/api/products`
- Ожидаемый статус: `200`

## После деплоя

### 1. Проверка работы
Откройте ваш домен и убедитесь, что:
- ✅ Главная страница загружается
- ✅ Каталог показывает товары
- ✅ Корзина работает
- ✅ Можно оформить заказ

### 2. Инициализация базы данных
База данных создастся автоматически при первом запуске и наполнится 20 товарами.

### 3. Тест Telegram (если настроен)
Проверьте уведомления:
```
https://ваш-домен.com/api/test-telegram
```

## Обновление приложения

Coolify автоматически пересобирает и деплоит при push в репозиторий:

```bash
git add .
git commit -m "Update application"
git push
```

Или вручную в интерфейсе Coolify: "Redeploy"

## Резервное копирование

### База данных
Регулярно делайте бэкап файла `/app/data/parrot_shop.db`:

```bash
# Через Coolify CLI или SSH
docker cp container_name:/app/data/parrot_shop.db ./backup/
```

### Автоматический бэкап
Настройте в Coolify автоматическое резервное копирование volume с базой данных.

## Мониторинг

### Логи
Просматривайте логи в Coolify:
- Application logs
- Build logs
- Deployment logs

### Метрики
Coolify показывает:
- CPU usage
- Memory usage
- Network traffic

## Решение проблем

### База данных не создается
1. Проверьте права доступа к `/app/data`
2. Убедитесь, что volume правильно примонтирован
3. Проверьте переменную `DATABASE_PATH`

### Приложение не запускается
1. Проверьте логи сборки
2. Убедитесь, что все зависимости установлены
3. Проверьте переменные окружения

### Порт недоступен
1. Убедитесь, что `PORT=5000` в переменных
2. Проверьте настройки reverse proxy в Coolify
3. Проверьте firewall правила

## Масштабирование

### Горизонтальное
Coolify поддерживает запуск нескольких реплик:
1. Настройте shared volume для базы данных
2. Или используйте PostgreSQL вместо SQLite
3. Увеличьте количество реплик в настройках

### Вертикальное
Увеличьте ресурсы контейнера:
- CPU limits
- Memory limits

## Дополнительно

### SSL/TLS
Coolify автоматически настраивает Let's Encrypt сертификаты.

### Домен
Привяжите свой домен в настройках проекта Coolify.

### CI/CD
Настройте webhooks для автоматического деплоя при push.

---

**Готово!** 🎉 Ваш магазин развернут на Coolify.
