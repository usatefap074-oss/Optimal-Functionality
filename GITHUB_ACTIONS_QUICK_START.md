# ⚡ Quick Start: GitHub Actions деплой

**Общее время настройки:** ~30 минут

## Шаг 1️⃣: SSH ключи (локальная машина)

```bash
# Сгенерировать ключ
ssh-keygen -t ed25519 -C "github-actions@parrot-shop" -f ~/.ssh/parrot_shop_deploy -N ""

# Посмотреть публичный ключ (скопировать для следующего шага)
cat ~/.ssh/parrot_shop_deploy.pub
```

## Шаг 2️⃣: На сервер (144.31.212.184)

```bash
# Подключиться
ssh root@144.31.212.184
# Пароль: eh5gRDe4yCsK

# На сервере:
mkdir -p ~/.ssh && chmod 700 ~/.ssh
cat >> ~/.ssh/authorized_keys
# Вставить публичный ключ из Шага 1, нажать Ctrl+D
chmod 600 ~/.ssh/authorized_keys
exit
```

## Шаг 3️⃣: Тест SSH (локальная машина)

```bash
# Должно работать без пароля
ssh -i ~/.ssh/parrot_shop_deploy root@144.31.212.184 "echo OK"
# Результат: OK
```

## Шаг 4️⃣: GitHub Secrets

1. Открыть: **GitHub → Settings → Secrets and variables → Actions**
2. Добавить каждый:

| Имя | Значение |
|-----|----------|
| `SSH_PRIVATE_KEY` | Содержимое `~/.ssh/parrot_shop_deploy` |
| `SERVER_HOST` | `144.31.212.184` |
| `SERVER_USER` | `root` |
| `DEPLOY_PATH` | `/opt/parrot-shop` |

## Шаг 5️⃣: Тест деплоя

```bash
# Коммитить изменения (они уже добавлены)
git add .
git commit -m "Add GitHub Actions CI/CD"

# Либо создать feature branch для теста:
# git checkout -b test/ci-cd
# git push origin test/ci-cd

# Или прямо в main:
git push origin main

# Смотреть логи деплоя:
# GitHub → Actions → Deploy to VPS → выбрать запуск
```

## ✅ Проверить что работает

```bash
# API
curl http://144.31.212.184/api/products

# Health check
curl http://144.31.212.184/health
```

---

## 📍 Что произойдёт после этого

1. **Любой `git push` в main** → автоматический деплой
2. **Собирается в облаке** (GitHub Actions)
3. **Загружается на сервер** через SSH
4. **Перезапускается сервис**
5. **Telegram уведомление** (если настроено)

---

## 🆘 Если что-то не работает

1. **Проверить GitHub Actions логи:** GitHub → Actions → Deploy to VPS
2. **Проверить сервер:** `ssh root@144.31.212.184 "journalctl -u parrot-shop -n 30"`
3. **Откатиться:** `/opt/parrot-shop_backup_*` содержит прошлые версии

---

**Полная документация:** `GITHUB_ACTIONS_SETUP.md`
