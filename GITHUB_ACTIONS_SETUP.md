# 🚀 Настройка GitHub Actions для автоматического деплоя

Этот файл содержит пошаговые инструкции по настройке автоматического деплоя на VPS через GitHub Actions.

## ⚠️ Важно: Этапы настройки

Следуйте инструкциям ниже в точном порядке:

1. [Генерация SSH ключей](#шаг-1-генерация-ssh-ключей)
2. [Установка на сервер](#шаг-2-установка-публичного-ключа-на-сервер)
3. [Тестирование SSH](#шаг-3-тестирование-ssh-подключения)
4. [Добавление GitHub Secrets](#шаг-4-добавление-github-secrets)
5. [Тестирование деплоя](#шаг-5-первый-тестовый-деплой)

---

## ШАГ 1: Генерация SSH ключей

### Что это?
SSH ключи используются для безопасного подключения GitHub Actions к вашему серверу. Вместо пароля используется криптографически защищённый ключ.

### Выполнить на локальной машине:

```bash
# Генерировать новый SSH ключ (ed25519 - самый современный и безопасный)
ssh-keygen -t ed25519 -C "github-actions@parrot-shop" -f ~/.ssh/parrot_shop_deploy -N ""
```

Это создаст два файла:
- `~/.ssh/parrot_shop_deploy` - **ПРИВАТНЫЙ КЛЮЧ** (хранить в секрете!)
- `~/.ssh/parrot_shop_deploy.pub` - **ПУБЛИЧНЫЙ КЛЮЧ** (можно на сервер)

### Проверить что ключ создан:

```bash
# Должно быть две строки
ls -la ~/.ssh/parrot_shop_deploy*

# Просмотреть публичный ключ (для следующего шага)
cat ~/.ssh/parrot_shop_deploy.pub
```

---

## ШАГ 2: Установка публичного ключа на сервер

### Что это?
Публичный ключ копируется на сервер, чтобы сервер мог узнать GitHub Actions.

### Выполнить на локальной машине:

```bash
# Подключитесь к серверу (последний раз с паролем!)
ssh root@144.31.212.184
```

Вам будет предложено ввести пароль: `eh5gRDe4yCsK`

### На сервере (после подключения):

```bash
# Создать директорию SSH если не существует
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Перейти в директорию
cd ~/.ssh

# Создать файл authorized_keys если не существует
touch authorized_keys
chmod 600 authorized_keys

# Добавить публичный ключ
# Вариант 1 (если скопировали содержимое):
cat >> authorized_keys
# Вставьте содержимое ~/.ssh/parrot_shop_deploy.pub (из локальной машины)
# Нажмите Ctrl+D чтобы завершить

# Вариант 2 (быстрее):
echo "ssh-ed25519 AAAA... github-actions@parrot-shop" >> authorized_keys
# Замените "AAAA..." на ваш публичный ключ
```

### Проверить что ключ добавлен:

```bash
# На сервере - проверить что ключ есть
cat ~/.ssh/authorized_keys

# Проверить права на файлы
ls -la ~/.ssh/
# Должно быть: -rw------- для authorized_keys
# Должно быть: drwx------ для директории
```

### Выйти с сервера:

```bash
exit
```

---

## ШАГ 3: Тестирование SSH подключения

### Что это?
Проверяем что SSH подключение работает, прежде чем добавлять в GitHub.

### Выполнить на локальной машине:

```bash
# Тест подключения с приватным ключом (должно работать без пароля!)
ssh -i ~/.ssh/parrot_shop_deploy root@144.31.212.184 "echo 'SSH connection works!'"

# Должна быть ответ:
# SSH connection works!

# Если есть ошибка - проверьте:
# 1. Публичный ключ скопирован на сервер
# 2. Права на файлы правильные
# 3. SSH демон работает на сервере
```

---

## ШАГ 4: Добавление GitHub Secrets

### Что это?
GitHub Secrets - это переменные окружения, которые GitHub хранит в зашифрованном виде. Они используются в GitHub Actions workflows.

### Подготовка значений:

**Перед добавлением в GitHub**, соберите эти значения:

```bash
# 1. Получить содержимое приватного ключа (локальная машина)
cat ~/.ssh/parrot_shop_deploy
# Скопируйте весь вывод включая строки:
# -----BEGIN OPENSSH PRIVATE KEY-----
# ... содержимое ...
# -----END OPENSSH PRIVATE KEY-----
```

| Секрет | Значение |
|--------|----------|
| `SSH_PRIVATE_KEY` | Содержимое всего файла `~/.ssh/parrot_shop_deploy` |
| `SERVER_HOST` | `144.31.212.184` |
| `SERVER_USER` | `root` |
| `DEPLOY_PATH` | `/opt/parrot-shop` |
| `TELEGRAM_BOT_TOKEN` | Из `.env.local` TELEGRAM_BOT_TOKEN (опционально) |
| `TELEGRAM_CHAT_ID` | Из `.env.local` TELEGRAM_CHAT_ID (опционально) |

### Добавить Secrets в GitHub:

1. **Открыть репозиторий** на GitHub
   - Перейти по ссылке: https://github.com/YOUR_USERNAME/Optimal-Functionality

2. **Перейти в Settings:**
   - Нажмите вкладку **Settings** (в меню репозитория)
   - Слева выберите **Secrets and variables** → **Actions**

3. **Добавить каждый секрет:**
   - Нажмите зелёную кнопку **New repository secret**
   - В поле **Name** введите имя (например: `SSH_PRIVATE_KEY`)
   - В поле **Secret** введите значение
   - Нажмите **Add secret**

4. **Результат:**
   Должны быть добавлены все секреты:
   - ✅ SSH_PRIVATE_KEY
   - ✅ SERVER_HOST
   - ✅ SERVER_USER
   - ✅ DEPLOY_PATH
   - ✅ TELEGRAM_BOT_TOKEN (опционально)
   - ✅ TELEGRAM_CHAT_ID (опционально)

---

## ШАГ 5: Первый тестовый деплой

### Что это?
Тестируем что весь процесс деплоя работает перед тем как использовать на продакшене.

### Вариант 1: Тест на feature branch (рекомендуется)

```bash
# Локально: создать feature branch для теста
git checkout -b test/github-actions-setup

# Добавить все изменения
git add .github/
git add .kiro/steering/ci-cd.md
git add .gitignore
git add server/routes.ts

# Проверить что нет лишних файлов
git status
# Не должно быть:
# - .env файлов
# - SSH ключей
# - node_modules

# Коммитить изменения
git commit -m "Add GitHub Actions CI/CD workflows and Kiro steering rules"

# Запушить feature branch
git push origin test/github-actions-setup
```

### На GitHub:

1. **Перейти в Actions:**
   - GitHub → вкладка **Actions**
   - Выбрать **Deploy to VPS** workflow
   - Должна быть строка "test/github-actions-setup" in progress

2. **Смотреть логи:**
   - Нажмите на строку с запуском
   - Смотрите логи каждого step
   - Если всё зелёное (✅) - отлично!

3. **Если есть ошибка:**
   - Прочитайте логи error step-а
   - Проверьте GitHub Secrets
   - Проверьте SSH подключение локально

### После успешного теста на feature branch:

```bash
# Локально: переключиться на main
git checkout main

# Merge feature branch
git merge test/github-actions-setup

# Запушить в main (это автоматически запустит деплой!)
git push origin main
```

### После деплоя в main:

Проверить что всё работает:

```bash
# 1. Проверить что деплой успешен
# GitHub → Actions → Deploy to VPS → последний запуск должен быть зелёный

# 2. Проверить что приложение работает
curl http://144.31.212.184/api/products

# 3. Проверить health endpoint
curl http://144.31.212.184/health

# 4. Проверить что БД не повреждена
curl http://144.31.212.184/api/products | grep -o '"id"' | wc -l
# Должно быть больше чем 0 товаров

# 5. Проверить логи на сервере
ssh root@144.31.212.184 "journalctl -u parrot-shop -n 30"
# Не должно быть ошибок (красные ERROR строки)

# 6. Проверить backup создан
ssh root@144.31.212.184 "ls -la /opt/parrot-shop_backup_* | head -3"
# Должны быть несколько backup директорий с датой и временем
```

---

## Вариант 2: Прямо в main (если вы уверены)

```bash
# Если вы уверены что всё правильно настроено:
git checkout main
git add .github/ .kiro/steering/ci-cd.md .gitignore server/routes.ts
git commit -m "Add GitHub Actions CI/CD workflows and Kiro steering rules"
git push origin main

# ⚠️ Это сразу запустит деплой на продакшене!
# Смотрите логи на GitHub → Actions
```

---

## 🎯 Результат

После успешной настройки:

- ✅ Любой `git push` в `main` ветку автоматически деплоится на сервер
- ✅ GitHub Actions собирает код в облаке (Node.js 20)
- ✅ Загружает на сервер через SSH
- ✅ Перезапускает сервис systemd
- ✅ Отправляет Telegram уведомление (если настроено)
- ✅ Создаёт автоматический backup перед деплоем

---

## 🔧 Типичные проблемы

### "SSH connection failed"
```
Error: Permission denied (publickey)
```

**Решение:**
1. Проверить что публичный ключ на сервере:
   ```bash
   ssh root@144.31.212.184 "cat ~/.ssh/authorized_keys"
   ```
2. Проверить что `SSH_PRIVATE_KEY` секрет добавлен в GitHub Secrets
3. Проверить что в секрете весь ключ (включая BEGIN/END строки)

---

### "Build failed"
```
Error: npm ERR! 404 Not Found
```

**Решение:**
1. Проверить что `package-lock.json` в репозитории
2. Локально выполнить:
   ```bash
   npm ci
   npm run build
   npm run check  # TypeScript проверка
   ```

---

### "Service failed to start"
```
Job for parrot-shop.service failed
```

**Решение:**
1. Проверить логи на сервере:
   ```bash
   ssh root@144.31.212.184 "journalctl -u parrot-shop -n 50"
   ```
2. Проверить что `.env` файл на сервере:
   ```bash
   ssh root@144.31.212.184 "cat /opt/parrot-shop/.env"
   ```
3. Проверить что `DATABASE_PATH` в `.env` правильный

---

## 📚 Дополнительная информация

- **Документация CI/CD:** `.kiro/steering/ci-cd.md`
- **Workflow файлы:** `.github/workflows/deploy.yml`, `.github/workflows/deploy-images.yml`
- **Переменные окружения:** `.env.example`

---

## ✅ Чеклист

После завершения настройки проверьте:

- [ ] SSH ключи сгенерированы (`~/.ssh/parrot_shop_deploy*`)
- [ ] Публичный ключ на сервере (`~/.ssh/authorized_keys`)
- [ ] SSH подключение работает локально
- [ ] Все GitHub Secrets добавлены
- [ ] Feature branch тестировал успешно
- [ ] Merge в main выполнен
- [ ] Деплой в main прошёл успешно
- [ ] Приложение доступно: `http://144.31.212.184`
- [ ] API работает: `http://144.31.212.184/api/products`
- [ ] Health endpoint работает: `http://144.31.212.184/health`
- [ ] БД не повреждена (товары на месте)
- [ ] Telegram уведомление получено (если настроено)

---

## 💡 Советы

1. **Не делитесь приватным ключом** - его нельзя восстановить
2. **Регулярно проверяйте логи** после деплоя
3. **Используйте feature branches** для тестирования
4. **Имейте план отката** (backups создаются автоматически)
5. **Мониторьте здоровье приложения:** `watch -n 5 'curl -s http://144.31.212.184/health'`

---

Если что-то не работает - проверьте логи на GitHub Actions или на сервере!
