# 🦜 Магазин товаров для попугаев

E-commerce платформа для продажи товаров для попугаев с каталогом, корзиной и оформлением заказов.

## 🚀 Быстрый старт

```bash
# Дважды кликните на start.bat
# ИЛИ выполните команды:

npm install
npm run dev
```

Откройте http://localhost:5000

**Всё!** База данных SQLite создастся автоматически и наполнится 20 товарами.

## 📦 Что включено

### Товары (20 шт.)
- **Корма** (6): Padovan, Versele-Laga, JR Farm, RIO, Fiory, Witte Molen
- **Клетки** (5): Triol, Ferplast, Savic, Imac, Voltrega
- **Игрушки** (5): Канаты, качели, лесенки, мячи, головоломки
- **Ветаптека** (4): Витамины, минералы, капли, пробиотики

Все товары с реальными изображениями (Unsplash) и описаниями на русском.

### Функции
- 🛍️ Каталог с фильтрацией и поиском
- 🛒 Корзина с сохранением состояния
- 📦 Оформление заказа с доставкой
- 💳 Выбор способа оплаты
- 💬 Подтверждение заказа через Telegram-бот
- 📱 Общение с менеджером в Telegram
- 📱 Адаптивный дизайн

## 🎨 Технологии

**Frontend:**
- React 18 + TypeScript
- Vite 7
- Tailwind CSS v4
- shadcn/ui (Radix UI)
- Zustand (state)
- TanStack Query
- Wouter (routing)

**Backend:**
- Node.js + Express 5
- SQLite + Drizzle ORM
- Zod validation
- Telegram Bot API

## ⚙️ Настройка

### Переменные окружения (`.env.local`)

```env
DATABASE_PATH=./data/parrot_shop.db
PORT=5000
NODE_ENV=development

# Telegram Bot (обязательно для подтверждения заказов)
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_BOT_USERNAME=ваш_username_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

### Настройка Telegram-бота

Telegram-бот используется для подтверждения заказов и общения с клиентами.

**Быстрая настройка:**

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен и username
3. Добавьте в `.env.local`
4. Получите Chat ID: http://localhost:5000/api/telegram-setup
5. Перезапустите сервер

**Подробная инструкция:** [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md)

**Как работает:**
- После оформления заказа клиент получает ссылку на бота
- В боте клиент подтверждает заказ кнопкой
- Менеджер получает уведомление
- Клиент может общаться с менеджером прямо в боте

## 🛠️ Команды разработки

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm start        # Запуск production
npm run check    # Проверка TypeScript
npm run db:push  # Применить изменения схемы БД
```

## 📁 Структура проекта

```
├── client/          # React frontend (Vite + TypeScript)
│   ├── src/
│   │   ├── components/  # UI компоненты (shadcn/ui)
│   │   ├── pages/       # Страницы (Home, Catalog, Cart, Checkout)
│   │   ├── hooks/       # React hooks (cart, products, orders)
│   │   └── lib/         # Утилиты
├── server/          # Express backend
│   ├── routes.ts    # API endpoints + seeding
│   ├── storage.ts   # Database queries (Drizzle)
│   ├── db.ts        # SQLite connection
│   └── telegram.ts  # Telegram notifications
├── shared/          # Общие типы и схемы
│   ├── schema.ts    # Drizzle tables + Zod schemas
│   └── routes.ts    # API route definitions
├── data/            # SQLite database
│   └── parrot_shop.db
└── start.bat        # Скрипт запуска
```

## 📝 API Endpoints

```
GET  /api/products              # Список товаров (с фильтрами)
GET  /api/products/:slug        # Товар по slug
GET  /api/products/id/:id       # Товар по ID
POST /api/orders                # Создать заказ
GET  /api/categories            # Список категорий
GET  /api/test-telegram         # Тест Telegram уведомлений
GET  /api/telegram-setup        # Получить Chat ID для настройки
POST /api/telegram/webhook      # Webhook для Telegram бота
```

## 🔧 Решение проблем

### База данных пустая
```bash
del data\parrot_shop.db
npm run db:push
npm run dev
```

### Порт 5000 занят
Измените `PORT` в `.env.local` на другой (например, 3000)

## 🌐 Деплой

### Coolify
Проект готов к деплою на Coolify. Подробная инструкция: [DEPLOY.md](./DEPLOY.md)

**Быстрый старт:**
1. Подключите Git-репозиторий в Coolify
2. Выберите Dockerfile или Docker Compose
3. Добавьте переменные окружения (см. `.env.example`)
4. Настройте persistent volume для `/app/data`
5. Deploy!

### Другие платформы
Проект также совместим с:
- Railway
- Render
- Fly.io
- DigitalOcean App Platform
- Любой платформой с поддержкой Docker

## 📄 Лицензия

MIT

---

**Готово к работе!** 🚀 Запустите `start.bat` или деплойте на Coolify
