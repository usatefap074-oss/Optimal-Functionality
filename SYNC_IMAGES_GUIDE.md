# 🖼️ Руководство по синхронизации изображений товаров

## Описание

Скрипт `sync-all-images.ts` полностью синхронизирует все изображения товаров с сайтом-донором (https://zolotoikakadu23.ru/parrot/).

**Что делает скрипт:**
- ✅ Подключается к сайту-донору через браузер
- ✅ Парсит все товары и их изображения
- ✅ Скачивает ВСЕ изображения каждого товара (не только основное)
- ✅ Сохраняет изображения локально в `client/public/images/products/`
- ✅ Обновляет БД с локальными путями
- ✅ Показывает прогресс в реальном времени

## Использование

### Быстрый старт

```bash
npm run sync:images
```

Скрипт автоматически:
1. Загружает сайт-донор
2. Прокручивает страницу для загрузки всех товаров
3. Парсит все изображения
4. Скачивает их локально
5. Обновляет БД

### Пример вывода

```
🔄 === СИНХРОНИЗАЦИЯ ВСЕХ ИЗОБРАЖЕНИЙ ТОВАРОВ ===

🌐 Подключаюсь к сайту-донору для получения всех изображений...

⏳ Загружаю товары...
📜 Прокручиваю страницу для загрузки всех товаров...
✓ Страница полностью загружена

✓ Найдено товаров на сайте: 63

📊 Товаров в БД: 63

📦 1/63 Попугай Ара красный
   Изображений на сайте: 3
   ⏳ Изображение 1/3... ✓
   ⏳ Изображение 2/3... ✓
   ⏳ Изображение 3/3... ✓
   ✅ Обновлено в БД (3 изображений)

📦 2/63 Попугай Какаду белый
   Изображений на сайте: 2
   ⏳ Изображение 1/2... ✓
   ⏳ Изображение 2/2... ✓
   ✅ Обновлено в БД (2 изображений)

...

==================================================
✅ СИНХРОНИЗАЦИЯ ЗАВЕРШЕНА!
==================================================
📥 Всего скачано изображений: 156
🔄 Обновлено товаров в БД: 63
❌ Ошибок: 0
📁 Изображения сохранены в: /path/to/client/public/images/products

💡 Следующий шаг: npm run build
```

## Как это работает

### Шаг 1: Подключение к сайту-донору
Скрипт использует Playwright для открытия браузера и загрузки сайта.

### Шаг 2: Парсинг товаров
Скрипт прокручивает страницу полностью, чтобы загрузить все товары (ленивая загрузка).

### Шаг 3: Извлечение изображений
Для каждого товара скрипт ищет:
- Основное изображение (из `data-original` или `src`)
- Дополнительные изображения из галереи

### Шаг 4: Загрузка изображений
Каждое изображение скачивается и сохраняется с именем:
- `product-{id}.jpg` - основное изображение
- `product-{id}-1.jpg` - второе изображение
- `product-{id}-2.jpg` - третье изображение
- и т.д.

### Шаг 5: Обновление БД
Для каждого товара обновляются поля:
- `image` - путь к основному изображению
- `images` - JSON массив со всеми путями

## Структура файлов

После синхронизации структура будет:

```
client/public/images/products/
├── product-1.jpg          # Основное изображение товара 1
├── product-1-1.jpg        # Второе изображение товара 1
├── product-1-2.jpg        # Третье изображение товара 1
├── product-2.jpg          # Основное изображение товара 2
├── product-2-1.jpg        # Второе изображение товара 2
├── product-3.jpg          # Основное изображение товара 3
└── ...
```

## Данные в БД

Для каждого товара в таблице `products`:

```json
{
  "id": 1,
  "name": "Попугай Ара красный",
  "image": "/images/products/product-1.jpg",
  "images": "[
    \"/images/products/product-1.jpg\",
    \"/images/products/product-1-1.jpg\",
    \"/images/products/product-1-2.jpg\"
  ]"
}
```

## Использование в приложении

### Основное изображение
```typescript
<img src={product.image} alt={product.name} />
```

### Галерея всех изображений
```typescript
const images = JSON.parse(product.images);
images.forEach(img => {
  <img src={img} alt={product.name} />
});
```

### В компоненте ProductCard
```typescript
export function ProductCard({ product }: ProductCardProps) {
  const images = JSON.parse(product.images);
  
  return (
    <div>
      <img src={product.image} alt={product.name} />
      {/* Галерея */}
      <div className="flex gap-2">
        {images.map((img, idx) => (
          <img key={idx} src={img} alt={`${product.name} ${idx}`} />
        ))}
      </div>
    </div>
  );
}
```

## Что делать после синхронизации

### 1. Проверить результаты
```bash
# Проверить что изображения скачаны
ls -la client/public/images/products/

# Проверить что БД обновлена
npm run dev
# Откройте http://localhost:5173/catalog
# Проверьте что изображения загружаются
```

### 2. Собрать для production
```bash
npm run build
```

### 3. Развернуть на сервер
```bash
npm run deploy
# или
npm start
```

## Возможные проблемы

### Проблема: "Изображения не скачиваются"
**Решение:**
- Проверьте интернет соединение
- Проверьте что сайт-донор доступен: https://zolotoikakadu23.ru/parrot/
- Попробуйте запустить скрипт снова

### Проблема: "Ошибка при парсинге"
**Решение:**
- Сайт-донор может изменить структуру HTML
- Обновите селекторы в скрипте `script/sync-all-images.ts`
- Проверьте консоль браузера для отладки

### Проблема: "Недостаточно места на диске"
**Решение:**
- Удалите старые изображения: `rm -rf client/public/images/products/*`
- Проверьте свободное место: `df -h`
- Используйте сжатие изображений

### Проблема: "БД не обновляется"
**Решение:**
- Проверьте что БД не заблокирована
- Остановите dev сервер: `Ctrl+C`
- Запустите скрипт снова
- Проверьте права доступа к файлу БД

## Оптимизация

### Сжатие изображений
После синхронизации можно сжать изображения:

```bash
# Установить ImageMagick
brew install imagemagick  # macOS
apt-get install imagemagick  # Linux

# Сжать все изображения
mogrify -quality 85 -resize 800x800 client/public/images/products/*.jpg
```

### Кэширование
Изображения кэшируются браузером автоматически благодаря заголовкам HTTP.

## Команды

| Команда | Описание |
|---------|---------|
| `npm run sync:images` | Синхронизировать все изображения |
| `npm run scrape:parrots` | Только парсить товары (без загрузки) |
| `npm run import:parrots` | Импортировать товары в БД |
| `npm run fix-images` | Исправить пути изображений (старый способ) |

## Статистика

После синхронизации вы получите:
- **63 товара** (примерно)
- **150-200 изображений** (в зависимости от количества фото на товар)
- **Размер:** ~50-100 MB (в зависимости от качества)

## Безопасность

- ✅ Скрипт не удаляет существующие файлы
- ✅ Скрипт не изменяет другие данные в БД
- ✅ Скрипт использует HTTPS для загрузки
- ✅ Скрипт проверяет статус HTTP ответа

## Поддержка

Если возникли проблемы:
1. Проверьте логи скрипта
2. Посмотрите консоль браузера (если используется Playwright)
3. Проверьте файл БД: `data/parrot_shop.db`
4. Обратитесь к документации проекта

---

**Готово! Теперь все изображения синхронизированы с сайтом-донором.** 🎉
