# 🦜 Запуск проекта - Магазин товаров для попугаев

## Быстрый старт (один клик)

**Дважды кликните** на файл `start.bat` в папке проекта.

Скрипт автоматически:
- ✓ Проверит Node.js и npm
- ✓ Установит зависимости (если нужно)
- ✓ Создаст .env.local (если нужно)
- ✓ Создаст SQLite базу данных (если нужно)
- ✓ Запустит dev сервер
- ✓ Наполнит базу 20 товарами (при первом запуске)

---

## Доступ к приложению

После запуска откройте в браузере:
- **Frontend**: http://localhost:5000
- **API**: http://localhost:5000/api
- **Товары**: http://localhost:5000/api/products

---

## Требования

### Обязательно установить:
- **Node.js** (v18+) - https://nodejs.org/

**Всё!** PostgreSQL не нужен - используется локальная SQLite база данных.

### Переменные окружения

Скрипт создаст файл `.env.local`:

```env
DATABASE_PATH=./data/parrot_shop.db
PORT=5000
NODE_ENV=development
TELEGRAM_BOT_TOKEN=ваш_токен_бота
```

**Настройка:**
- `DATABASE_PATH` - путь к SQLite базе (по умолчанию `./data/parrot_shop.db`)
- `PORT` - порт сервера (по умолчанию 5000)
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота для уведомлений о заказах (опционально)

---

## Остановка

Нажмите **Ctrl+C** в окне консоли

---

## Ручной запуск

```bash
# Установка зависимостей
npm install

# Создание базы данных (первый раз)
npm run db:push

# Запуск dev сервера
npm run dev
```

---

## Решение проблем

### "Node.js не найден"
- Установите Node.js с https://nodejs.org/
- Перезагрузитесь после установки

### Порт 5000 уже занят
- Измените PORT в `.env.local` на другой (например, 3000)
- Или закройте приложение, которое использует порт 5000

### База данных пустая
```bash
# Удалите базу и пересоздайте
del data\parrot_shop.db
npm run db:push
npm run dev
```

---

## Команды разработки

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm start        # Запуск production версии
npm run check    # Проверка TypeScript
npm run db:push  # Применить изменения схемы БД
```

---

## База данных

Проект использует **SQLite** - локальную файловую базу данных:
- Файл: `./data/parrot_shop.db`
- Не требует установки PostgreSQL
- Автоматически создаётся при первом запуске
- Содержит 20 товаров с картинками и описаниями

### Товары в базе:
- **Корма** (6): Padovan, Versele-Laga, JR Farm, RIO, Fiory, Witte Molen
- **Клетки** (5): Triol, Ferplast, Savic, Imac, Voltrega
- **Игрушки** (5): Канаты, качели, лесенки, мячи, головоломки
- **Ветаптека** (4): Витамины, минералы, капли, пробиотики

---

## Telegram уведомления (опционально)

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен
3. Добавьте токен в `.env.local`:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_здесь
   ```
4. Перезапустите сервер
5. Протестируйте: http://localhost:5000/api/test-telegram

---

**Готово! Приложение должно запуститься автоматически.** 🚀
